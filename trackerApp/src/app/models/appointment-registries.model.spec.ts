import { AppointmentRegistries } from './appointment-registries.model';

describe('AppointmentRegistries', () => {
  it('should create an instance', () => {
    expect(new AppointmentRegistries()).toBeTruthy();
  });
});
